INCLUDE_DIRECTORIES(.)

FILE(GLOB_RECURSE SRC_FILES "*.cpp")

ADD_EXECUTABLE(game_engine ${SRC_FILES})
SET(EXECUTABLE_OUTPUT_PATH ${PROJECT_SOURCE_DIR}/bin-${ENGINE_BUILD_TYPE})

TARGET_LINK_LIBRARIES(game_engine lua)
